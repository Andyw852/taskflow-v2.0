# =====================================================================
# step5_label 参数（DFT 单点标注，fanout cfg-*，12 核提交）
# =====================================================================

[params]
# INCAR 由 gen_step5_label.py 按 step1 输出自动推导（§8.1），
# 可在这用 [incar] / [incar.final] 覆盖个别标签。

[submit]
# 12 核小作业（好排队）。单帧排队快、吞吐靠 fanout 并行。
nodes           = 1
ntasks_per_node = 12
# time          = 12:00:00    # 大超胞单点加时用

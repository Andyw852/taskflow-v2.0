# =====================================================================
# step1_relax 参数（mlff-mace 紧弛豫，12 核提交）
# 改：tf -tt mlff-mace -p <材料> -j 1 conf --set submit.ntasks_per_node=24
# =====================================================================

[params]
# 本步策略（三段式 EDIFFG=-0.001）在 gen_step1_relax.py 里写死——
# 那是本技能与其它技能弛豫策略的本质区别，不开放成参数。

[submit]
# 12 核小作业（好排队）：模板里 ntasks-per-node=24 会被这里覆盖。
# 想加大：tf conf --set submit.ntasks_per_node=24
nodes           = 1
ntasks_per_node = 12

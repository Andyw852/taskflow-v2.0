# =====================================================================
# phonon-dft-cpu 全局 step.conf —— 只放各步骤共用的键
# 每个步骤自己的参数放 per-step 文件（templates/<步骤名>/step.conf）。
# 三层合并，越靠后越优先：技能默认 → 材料共用 → 材料本步。
# =====================================================================
[params]
FUNC = pbesol

# step2_static 参数（原胞静态自洽）。改：tf -tt kl-dft-cpu -p <材料> -j step2_static conf --set params.KSPACING=0.025
[params]
FUNC     = pbesol        # 与 step1 一致；auto=继承 step1 的 workflow_method.txt
KSPACING = 0.03          # 静态密网格
[submit]
# nodes = 1              # 需要就取消注释覆盖 submit.sh 的 #SBATCH

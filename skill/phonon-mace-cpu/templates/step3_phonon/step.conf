# step3_phonon 参数（symfc 拟合 2 阶 + 声子谱）。
[params]
FC_CUTOFF = 6.0
# 虚频闸阈值由 driver 内置（-0.10 THz）。stable_mesh 保留旧 mesh 判据；stable_new 使用 mesh/band 合并判据。
# MAX_FREQ_MIN_THZ：有限正数时启用最高频率下限检查；留空关闭。
MAX_FREQ_MIN_THZ = 20.0

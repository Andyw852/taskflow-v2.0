# taskflow-v1.0 路线图 —— "加技能友好" 改造

> 本副本（`~/software/taskflow-v1.0`）是 `taskflow-v2.0` 的**隔离开发副本**：
> v2.0 一行未改。隔离方式、安全约定见 `DEV-ISOLATION.md`。
> 起点：2026-09-14 那次讨论里的 14 条建议 + W1–12 排期；本文件记录**做了什么、
> 怎么用、还没做什么**。

---

## 一、已经做完（第一批 · 4 件核心里的 3 件 + W1–4 + W5–8）

| 建议 | 交付物 | 文件 | 怎么用 |
|---|---|---|---|
| **1.3 io_schema** | 技能自报"吃什么 / 吐什么 / 有哪些旋钮" | `tfpkg/skillspec.py`；样例 `skill/band-dft-cpu/skill.yaml` | `tf schema band-dft-cpu` |
| **1.2 flow** | 技能自报"整条流程干什么 / 产物能喂给谁" | 同上 | 同上（`flow.stages` / `flow.next_skills`） |
| **1.1 _corrections/** | 纠错 handler 库：诊断特征 → 该动什么 | `tfpkg/corrections.py` + `skill/_common/_corrections/`（base + 4 handler + README） | `tf correct -p 材料 [-j 步骤] [-y]`；`tf diagnose` 里自动带建议 |
| **3.1 自动发现** | 零配置：`skill/<名>/skill.yaml` 放进目录即生效 | 已有机制（`discover_skills`）+ 本次补齐**自描述的自动校验**与展示 | `tf skills`（多一列「自描述」）、`tf schema` |
| **W1–4 `tf schema`** | 技能自描述的查看/校验入口 | `tfpkg/skillspec.py: cmd_schema` | `tf schema [技能] [--json] [--strict]` |
| **W5–8 `history.jsonl`** | 步骤状态时间序列，**任何技能自动就有历史** | `tfpkg/history.py` | `tf history [-p 材料] [-tt 技能] [--since 7d] [-n 40] [--json]` |
| 模板 | 新技能照着复制 30 分钟上线 | `skill/_template/`（skill.yaml + gen 脚本 + README 清单） | `cp -r skill/_template skill/我的技能` |

### 1.1 命令速览

```bash
python3 bin/tf skills                      # 技能表：版本/步骤数/自描述摘要/问题数
python3 bin/tf schema                      # 全部技能的自描述覆盖率一览
python3 bin/tf schema band-dft-cpu         # 单技能：io_schema + flow + corrections + 校验
python3 bin/tf schema band-dft-cpu --json  # 机器可读（工具/AI 判读）
python3 bin/tf schema --strict             # 自描述有 [错误] 时返回非零（可进 CI）

python3 bin/tf diagnose -p <材料>          # 诊断 JSON 里每步多带 corrections 建议
python3 bin/tf correct  -p <材料> -j S1_opt       # 只读：命中哪些 handler、该跑哪条命令
python3 bin/tf correct  -p <材料> -j S1_opt -y    # 执行 handler.apply（改远端 INCAR，自动备份）

python3 bin/tf history -p <材料>           # 该材料的状态时间线
python3 bin/tf history --since 7d -n 100   # 最近 7 天、100 条
```

### 1.2 三个"能力"各自解决什么

**io_schema（建议 1.3）** —— 老 skill.yaml 只描述"怎么跑"，不描述"吃什么/吐什么"。
现在技能可以自报：

```yaml
io_schema:
  inputs:  [{name: POSCAR, from: user, required: true, desc: ...}]
  outputs: [{name: WAVECAR, step: S3_WAVECAR, desc: ...}]
  params:  [{name: BANDGAP, values: [pbe, hse], default: hse, where: step.conf}]
```

校验会抓：缺 name（错误）、未知字段（警告）、`from: step:xxx` 指向不存在的步骤（警告）、
`default` 不在 `values` 里（警告）……**写错当场告诉你**，而不是等人踩坑。

**flow（建议 1.2）** —— 把"这条流水线在干什么、产物能喂给谁"写进仓库：

```yaml
flow:
  summary: 结构弛豫 → PBE 自洽 → 波函数 → [HSE] → 真空对齐
  stages: [{name: 弛豫, steps: [S1_opt], produces: [CONTCAR]}]
  next_skills: [{skill: kl-dft-cpu, via: CONTCAR}]
```

**\_corrections/（建议 1.1）** —— 把"这个报错该怎么办"从人的脑子里搬进代码：

| 诊断特征 | handler | 风险 | 动作 |
|---|---|---|---|
| ZBRENT / EDDAV / SCF 空转 | `zbrent` | review | retry；连续失败则打 INCAR 补丁（AMIX/BMIX→ALGO=All→NELM≥200） |
| force not converged / relax_nsw | `nsw_continue` | safe | retry 续算（自动 cp CONTCAR） |
| NODE_FAIL / 被抢占 | `node_fail` | safe | retry 重交 |
| dir missing | `dir_missing` | destructive | 只给建议；rerun 必须人工执行 |

★ `tf correct` **永不提交作业、永不删目录**：`apply()` 只允许改输入文件（备份 + 原子写），
提交仍走 `tf start`、重生成仍走 `tf retry/rerun`。这是刻意的纪律（对齐 AGENTS.md 铁律 1）。

**history.jsonl（W5–8）** —— 记录是**自动**的：任何一次真正采集之后，状态转移就追加进
`setting/history.jsonl`（首次采集只落基线，避免给几千个材料刷一屏"首次见到"）。
字段：`ts / mat / skill / step / ev(state|diag) / f / t / diag / job / host`。
文件超过 8MB 自动裁到最近 4 万行。

---

## 二、兼容性与风险控制

| 项 | 做法 |
|---|---|
| 老技能 | `schema: 1` 完全不受影响（新段不参与运行期装配，老 tf 只会忽略） |
| 新段是"自描述" | `io_schema/flow/corrections` **不改变任何执行行为**，只影响 `tf schema/skills` 展示与校验 |
| 纠错库加载 | 按需加载（只有 `tf schema/diagnose/correct` 会 import）；加载失败不影响 tf 启动，只是少几条建议 |
| 技能私有 handler | `skill/<技能>/_corrections/` 只对该技能生效，不会误伤别的技能（有单测覆盖） |
| 新命令只读 | `tf schema / history` 纯本地、不连超算；`tf correct` 不带 `-y` 也是只读 |
| 回归自测 | `tmp/test_v1_skillspec.py`（30 项）+ `tmp/test_v1_history.py`（17 项），全部离线、不连超算 |

---

## 三、还没做（下一批）

### 3.1 W9–12：`result/_cache/` 结果缓存（设计草案，未实现）

**目标**：筛选型技能（`te-screen`、`opt-mace-*`、`phonon-mace-*`）在成千上万材料上重复
算"同一个结构的同一个步骤"时，直接命中缓存、不再排队。

**键（必须包含一切影响数值的东西）**：

```
key = sha256( skill_key | step_name | skill_version
              | POSCAR 归一化内容
              | INCAR/KPOINTS/step.conf 生效值（规范化排序后的 key=value）
              | POTCAR 标识（potcar_dir + 元素列表 + 文件大小/哈希）
              | hpc 名 + 提交模板版本 )
```

**存放**：`result/_cache/<技能>/<步骤>/<key 前 16 位>/`，内含产物 + `meta.json`
（键组成、生成时间、来源材料、墙钟耗时、VASP/MACE 版本）。

**写入**：步骤 done + fetch 成功时落缓存（只对**确定性**步骤；随机/含时间戳的步骤不缓存）。

**命中**：`tf start` 时先算键 → 命中则把产物复制进步骤目录、标记 done，
状态表显示 `hit`（**不提交作业**）；`--no-cache` / `-f` 强制真算。

**失效**：输入/模板/技能版本变化自动失效（键变了）；显式失效走 `tf clean`；
容量上限（LRU / 单步配额）+ `tf cache stat|prune` 管理命令。

**风险**：POTCAR/泛函/集群不同 → 键必须覆盖（否则会"用错结果"）；因此**默认关闭**，
按技能/步骤 opt-in（skill.yaml 步骤上写 `cache: true`），先只读上线（`tf cache stat` 能看命中率），
再开写入、最后开命中。

### 3.2 其余待办

1. **给另外 15 个技能补三段自描述**（现在只有 `band-dft-cpu` 作为活样例）——
   建议一个技能一个 commit，用 `tf schema --strict` 当验收；
2. **`flow.next_skills` 做成"串技能"建议**：`tf schema` 里反向索引"谁的产物能喂给我"，
   进一步让 `tf` 能提示"这个材料的 opt 算完了，可以接 phonon"；
3. **`io_schema` 参与 `tf init` 校验**：初始化项目时检查"声明的必需输入是否存在"；
4. **history 的统计视图**：每技能/每集群的"排队时长分布 / 一轮通过率 / FAIL 原因分布"，
   给"加技能"提供回归基线；
5. **`tf correct` 与 `hang_check` 合并**：目前 `hang_fix_scf` 是自动路径、`tf correct` 是手动路径，
   两者已共用 `_hung_incar_fix`，后续可让 monitor 直接调 handler 的 `auto=True` 建议。

---

## 四、怎么把成果搬回 v2.0（或另开仓库）

本副本是 git 仓库（分支 `dev-v1.0`，远端已改名 `origin-v2.0` 防误 push）：

```bash
cd ~/software/taskflow-v1.0
git log --oneline dev-v1.0          # 看提交
git diff master..dev-v1.0 --stat    # 看对 v2.0 的全部改动（含基线快照里的旧未提交改动）
git diff 4785420..dev-v1.0 --stat   # 只看"隔离之后我自己改的"（4785420 = 基线快照）
```

搬回去的两条路：
- **只搬特性**：把 `tfpkg/skillspec.py`、`tfpkg/corrections.py`、`tfpkg/history.py`、
  `skill/_common/_corrections/`、`skill/_template/` 复制进 v2.0，再打 `tfpkg/__init__.py`、
  `cli.py`、`bootstrap.py`、`report.py`、`ops.py` 那几处小改动（`git diff` 里一目了然）；
- **整仓切换**：确认副本的 `setting/`（站点配置）与你生产用的一致后，把 `tf` 指向副本
  （`~/.local/bin/tf`）——**注意先备份 v2.0**。

---

# 第二批：给论文提供证据（P0-2 / P0-3 落地）

> 对照"论文大纲 × 仓库实际能力"的逐条核对，本批做的是**平台层已经够用、缺的是证据链**的部分。
> 全部是加法：老技能（schema 1）零改动照跑；新段/新文件不参与运行期装配。

## 5.1 已完成

| 论文缺口 | 交付物 | 入口 | 验证 |
|---|---|---|---|
| **P0-3** skill 声明式 I/O + 内省命令 | `io_schema.steps[]`（每步 title/tool/generator/validator/inputs/outputs）+ `tf skill show` 渲染成**技能卡片**，版式与论文图 2 逐项对齐：外框 + `Skill: <标题>` + tagline，一行 4 个步骤盒（Step N / 标题 / Generator / Tool / Validator / Output），盒间 `→`，页脚 `Skill definition: …` 与 `Execution backend: …`（`flow.title/tagline/backend` 三个可选键控制抬头与工具链，缺省自动推导）；`--full` 追加输入/参数/可接技能/纠错 | `tfpkg/skillspec.py`；`tf skill [show] [技能] [--json] [--full]` | `kl-dft-cpu`（7 步）/ `kl-mace-gpu`（5 步，与图 2 四步同构）实测；`--strict` 全库 0 问题 |
| **P0-2** 统一 per-step provenance | gen 推送阶段 tf **自动**写 `<材料>/provenance/<步骤>.json`（输入文件 sha256、step.conf 合并后的参数、hpc/远端路径、生成器 sha256、tf 版本）+ 时间线 `provenance/history.jsonl`；脚本侧可用公共池 `provenance_common.py` 补工具版本/产物/作业号；只读查看 `tf prove -p 材料 [--verify]` | `tfpkg/prov.py`、`skill/_common/provenance_common.py`、`workflow.remote_gen` 挂钩 | `tmp/test_v1_prov.py` 22 项（本地 bash 模式真跑一遍 gen） |

### 5.2 关键设计决定（为什么这么做）

1. **provenance 由 tf 写，而不是"每个 gen 脚本自己写"**：gen 脚本大多是一层薄壳
   （`R.run(...)` 之后 `sys.exit`），插桩点不可靠；而 tf 在推送阶段**已经**知道
   推了哪些文件、内容是什么（base64 就在手里）→ 哈希是白送的。这样 19 个技能
   **一次覆盖**，且 gen 脚本一行不改。
2. **落在 `<材料>/provenance/<步骤>.json` 而不是步骤目录**：gen 的 cwd 是**材料目录**
   （`remote_gen` 的 step_dir），写单个 `provenance.json` 会被上一步覆盖。分步一份 +
   一行 JSONL 时间线，既有当前态也有历史。
3. **档案绝不阻断计算**：写档案整段包在 try/except 里，失败只打警告；开关
   `tf.yaml: provenance: false` 或 `TF_PROVENANCE=0`。测试里专门断言"关掉后 gen 仍成功"。
4. **schema 2 只做可选扩展**：`io_schema.steps` 不认识就忽略；19 个 schema 1 技能
   行为不变（`tf schema --strict` 全库仍 0 错误）。
5. **卡片直接来自声明，不手画**：`tf skill show kl-mace-gpu` 出来的就是
   "1 Relax structure ──▶ 2 Generate forces ──▶ 3 Fit force constants ──▶ 4 Solve transport"，
   与论文图 2 同构；改了 skill.yaml 卡片自动跟着变（图不会过期）。

## 5.3 本批进度（✅ = 已完成并自测；详见 §5.4b–§5.4e）

| 优先级 | 缺口 | 打算怎么做 | 依赖 |
|---|---|---|---|
| **P0-1** ✅ | LLM 动作网关 + 审计（`tf act`） | 已落地 `tfpkg/agentgate.py`（风险三档 + 一次性批准令牌 + `.tf_agent_log.jsonl` 审计）；`TF_ACTOR` 与 provenance 的 actor 打通。见 §5.4b；测试 `tmp/test_v1_agentgate.py` 40 项 ALL PASS | 无 |
| **P0-4** | 资源与精度基准聚合（`tf bench`） | 每步补 `resource_summary.json`（核数/wall/GPU 标志，提交模板与采集里加）→ 聚合出 method×backend×fit 的 CSV（图 4） | 需要真实跑批数据 |
| **P0-5** | 筛选漏斗与排名（`screening_report.json/csv`） | 读 `te-screen` 各阶段产物：每阶段剩余数、淘汰原因分布、最终排名（图 5a） | 需要 te-screen 实跑 |
| **P1-6** ✅ | per-step 事件流（submit/finish/retry/fail + jobid + walltime） | 已在 `history.jsonl` 上补 `ev=action`（init/start/retry/rerun/stop/fetch/gen/hang 自动恢复）与 `ev=finish|fail`（带墙钟 `dur`）。见 §5.4e | 无 |
| **P1-7** ✅ | `tf session export -p 材料` | 已落地 `tfpkg/session.py`：审计 + 步骤历史 + provenance + 状态快照打成 tar.gz（含逐文件 sha256 的 manifest）。见 §5.4c；测试 `tmp/test_v1_session.py` 28 项 ALL PASS | 依赖 P0-1（已具备） |
| **P1-8** | LLM 规划评测集（`eval/goals.yaml` + 打分脚本） | ~20 条自然语言目标 → 期望 skill/步骤；算技能选择正确率、顺序正确率、参数有效率；消融=只给目标 vs 目标+`tf summary` 反馈 | 需要 LLM 调用预算 |
| **P1-9** ✅ | 新技能 drop-in 测试 | 已自动化成 `tmp/test_v1_dropin.py`：两个投放点被发现 / 卡片可渲染 / schema 无错 / step1 真跑出产物并自动落 provenance / 核心代码 sha256 零变化。见 §5.4d | 无 |
| **P2** | MCP/REST 只读接口、corrections yaml 化、力计算缓存、Web 面板 | 放补充材料/投稿后 | — |

## 5.4b P0-1 动作网关（`tf act`）——"AI 不能自己决定破坏性动作"

**问题**：tf 分不清"人敲的"和"agent 敲的"。会话里一次手滑的 `tf rerun` 就
`rm -rf` 掉算完的步骤目录，而且事后查不到"谁、什么时候、下的什么手"
（材料目录的 tf.log 只记成功动作，被拒的调用根本不落）。

**做法**（`tfpkg/agentgate.py`，两层，都不改既有命令的行为）：

| 档 | 命令 | 策略 |
|---|---|---|
| 只读 | list/summary/status/dir/skills/skill/schema/history/prove/probe/config/diagnose/session，以及**任何带 `--dry-run` 的调用** | 放行 + 记账 |
| 推进 | start/retry/fetch/init/adopt/level/hpc/auto/`conf --set`/correct/monitor… | 放行 + 记账 |
| 破坏性 | stop/rerun/clean/migrate-subdir、`correct -y`，以及任何带 `-f/-y/--purge-config` 的调用 | **需人工批准令牌** |

- **人工批准**：人在**交互终端**跑 `tf approve <同一条命令>`（非 TTY 直接拒绝，
  这样 agent 无法自我批准）→ 按"命令签名"（命令词 + 参数，忽略顺序与 `-y`）发一张
  一次性令牌，默认 900 秒（`TF_APPROVE_TTL` 可调），用完即销、不可重放、过期作废；
- **审计**：配置目录下的 `.tf_agent_log.jsonl`，每次 `tf act` 追加
  `{ts,actor,cmd,argv,risk,decision,exit_code,dur,cwd,approved_by,sig}`；放行、拒绝、
  失败都记。设了 `TF_ACTOR` 的会话**直接**敲 tf 也记（`decision=direct`），
  并且缺省按严格档拦破坏性动作（`TF_AGENT_STRICT=0` 可关，给用户自己的定时脚本留门）；
- **零影响**：不设 `TF_ACTOR` 且不用 `tf act` 时，行为与改动前完全一致（有测试断言）；
- `tf act policy` 看分档表，`tf act log` 看流水。

**测试**：`tmp/test_v1_agentgate.py`（16 组 / 40 项断言，ALL PASS）覆盖：非 agent
零影响、agent 直连被拦、`--dry-run` 降级为只读且真不删东西、pt y 终端批准、
参数顺序不同仍命中同一签名、令牌一次性、令牌过期、`-y/-f` 一律按破坏性、
`-p` 写在 `act` 之前直接拒绝（防批准与执行不是同一条）、`act act` 不许嵌套。

## 5.4c P1-7 会话导出（`tf session export`）——论文的可复现性补充材料

```bash
python3 bin/tf session export -p Si_auto            # → tmp/session_Si_auto_<时间>.tar.gz
python3 bin/tf session export -p Si_auto --json     # 只打印总账，不写包
```

包里：`manifest.json`（tf 版本 / git 提交 / 时间 / 材料 / 技能 / 超算 / 每步状态 /
**逐文件 sha256 + 字节数** / 重放指引）、`README.txt`（怎么读、怎么重放）、
`status.txt`（人看的步骤表）、`history.jsonl`（步骤状态转移 + 动作事件 + 完成耗时）、
`agent_log.jsonl`（P0-1 的审计流水）、`provenance/<步骤>.json`（输入 sha256 /
参数 / 工具版本 / 作业号）。

边界：**全程只读本地**（不连超算、不提交）；只收已经 fetch 回本地的 provenance
（要全先 `tf -p X fetch`）；不含任何口令/密钥；tar 成员排序 + mtime 归零 → 同数据同包。
**测试**：`tmp/test_v1_session.py`（10 组 / 28 项断言，ALL PASS）。

## 5.4d P1-9 新技能 drop-in（自动化）

`tmp/test_v1_dropin.py`：把 `skill/_template/` 复制到两个投放点（配置目录旁的
`skill/`、`tf.yaml: skill_paths` 指向的目录），断言被自动发现、`tf skill show`
能渲染卡片、`tf schema --strict` 无错、第一步真能跑出 done_marker 并自动落
provenance，最后比对 `tfpkg/*.py` 与 `bin/tf` 的 sha256 —— **核心代码零改动**
（技能是数据，不是代码）。全程本地模式，不连超算。

## 5.4e P1-6 per-step 事件流（续：动作事件 + 墙钟）

`history.jsonl` 原来只有"状态转移"，答不了"谁重交的""这步实际多久"。现在：

- **动作事件 `ev=action`**：`log_action` 除写材料 `tf.log`，同时记
  `start/retry/rerun/stop/clean/init/fetch/gen/hang 自动恢复`，带材料/技能/步骤/
  作业号/集群/原文。从 tf 自己的日志文本解析，抠不出就留空，**不影响操作**；
- **完成事件 `ev=finish|fail` + `dur` 墙钟**：状态从 `R/PD` 落到 `OK/FAIL` 时记墙钟秒
  （起算点 = 第一次看到它 PD/R 的那轮），`tf history` 渲染成 `R → OK（耗时 12m03s）`。

真机验证：jzzn 上 `tf … init` 后 history 落 `{"ev":"action","act":"init",...}`；
本地把 6 类动作（gen/start/hang 自动恢复/retry/stop/fetch）逐条验过。

## 5.4 怎么用（本批新增命令）

```bash
# 技能卡片（论文图 2 的数据来源）
python3 bin/tf skill                      # 全部技能一行摘要（含工具链）
python3 bin/tf skill show kl-mace-gpu      # 卡片：4 步 × (Generator/Tool/Validator/Output)
python3 bin/tf skill show kl-mace-gpu --json   # 机器可读，供画图脚本直接用

# 每步"怎么来的"
python3 bin/tf prove -p <材料>              # 该材料所有已回拉步骤的档案
python3 bin/tf prove -p <材料> -j S2_static --verify   # 校验输入 sha256 有没有被改
python3 bin/tf prove -p <材料> --json       # 结构化（写补充材料/画图）
```

> 档案落在超算 `<材料>/provenance/`；随 fetch **自动**回拉本地 `result/provenance/<步骤>.json`
> （不用手动 fetch），步骤目录里脚本侧合并的那份则落在 `result/<步骤>/provenance.json`，
> `tf prove` 优先读后者（内容更全）。

## 5.4g 全技能 Si 冒烟顺手抓到的缺陷：技能里写死 `tf` 会打到**另一棵树**

`skill/cohp-cogito/gen_step1_cohp.py` 在"上游 band-dft-cpu 还没算"时会自动拉起上游：

```python
subprocess.run(["tf", "-tt", "band-dft-cpu", "-p", material, "start"], ...)
```

写死裸 `tf` 有两个坑：① 副本树一般用 `python3 bin/tf` 调用，PATH 里未必有 `tf`，
于是这条"自动拉起上游"直接报 `[Errno 2] No such file or directory: 'tf'`；
② 更糟的是 PATH 里**有** `tf` 时（`~/.local/bin/tf` 指向生产树），它会让**生产树**
拿着它自己的配置去提交——跨树副作用，属于最不该发生的一类。
**修法**（两棵树都改）：加 `_tf_bin()`，顺序是 `TF_BIN` 环境变量 → 同仓库
`bin/tf` → PATH 里的 `tf`。真机复验：Si 冒烟里 cohp-cogito 不再报"找不到 tf"，
而是正确地走到"上游未就绪，已拉起/请先跑 band-dft-cpu"这条分支。

## 5.4f 全技能 Si 冒烟（生成级，`tmp/test_v1_skills_si.py`）

用**同一个 Si 小体系**（金刚石 8 原子，`test/tf_test/Si/POSCAR`）把 18 个技能各跑一遍
「项目初始化 + 第一步输入生成」，在真集群上验证技能装配、gen 脚本、公共池依赖、
提交模板、step.conf 合并是否都还成立：

```bash
python3 tmp/test_v1_skills_si.py            # 全部 18 个技能
python3 tmp/test_v1_skills_si.py --tt kl-dft-cpu,opt-dft-cpu   # 指定技能
```

- 隔离配置 `tmp/tf_jzzn_si_all.yaml`：`project_roots` 只认 `/home/wangchao/tf_smoke_all`，
  auto 全关、`hang_check: false`、每技能 `max_jobs: 1`；
- 远端一律落在各集群的 `…/work/tf_smoke`（jzzn 13 个技能、3090 5 个 GPU/筛分类技能），
  与生产材料目录完全隔离；**不提交任何作业**（只 `init` 生成输入）；
- **判据**：`init` rc=0 且 gen rc=0 且**远端步骤目录非空**（只读 `ssh ls` 数个文件，
  避免"消息说成功、其实什么都没落"）；
- 结果表写到 `tmp/v1_si_all_skills.md`，逐技能完整输出在 `tmp/si_skills_logs/<技能>.log`。

> 跑之前会把远端测试根**清空重建**（只清 `…/work/tf_smoke`）：tf 把 gen 脚本/公共库
> 推到 `work_dir` 下的技能目录当"远端副本"，不清就会拿**旧副本**跑——本项目里实测
> 踩到过：残留的旧 `band-dft-cpu/gen_*` 让新材料的 gen 直接跳过（"输入文件已存在"）。

### 结果（2026-09-15 01:08，真集群）

**16 / 18 通过**（判据：`init` rc=0 且 gen rc=0 且远端步骤目录非空）。逐技能：band-dft-cpu 10 个
输入文件、defect 1（`references_energy.json`）、elastic 10、ke 10、kl 10、kl-mace-cpu/gpu 5、
mlff-mace 10、opt-dft-cpu 9、opt/kl/phonon-mace-cpu/gpu 各 5、phonon-dft-cpu 10、
te-screen 1、unihamgnn 9 —— 13 个技能在 jzzn、5 个在 3090，全部落在隔离的 `…/work/tf_smoke`。

另 2 项是**设计内拒绝**（不是技能坏了）：`cohp-cogito` 要 band-dft-cpu 的 step3_PBE_WAVECAR、
`fc-fit` 要位移+受力数据集；空 Si 体系里它们本就跑不出来，正确地拒绝并给出补齐命令才是
判据要的行为。完整表：`tmp/v1_si_all_skills.md`。

### 本轮由它抓出的两个真缺陷（都已修）

1. **`hpc` 是数字且没加引号时 `tf init` 会崩**（`tfpkg/ops.py`）：`_init_one_skill` 里
   `hpc_name + ".yaml"` 没做类型归一，只要 `hpc` 解析成 int 就
   `TypeError: unsupported operand type(s) for +: 'int' and 'str'`。
   **触发条件**：配置里写 `task_types.<技能>.hpc: 3090`（不加引号 → YAML 给 int）。本次冒烟的
   隔离配置是脚本生成的，正好是这种写法，于是 te-screen、unihamgnn、kl/opt/phonon-mace-gpu
   五个技能 `init` 全崩、整轮只过 11/18。
   **更正一句容易说错的话**：技能出厂配置不受影响（`skill.yaml` 里是 `hpc: "3090"`，带引号），
   所以这不是"开箱即崩"，而是"自己写配置把 3090 不加引号就崩"。但它确实是本项目**已知会踩的
   坑**——`setting/3090.yaml` 专门注明"name 值要加引号，避免被解析成整数"，`bootstrap.py`
   早有一处 `str()` 补丁，漏的正是 `ops.py` 这一处。
   修法：`hpc_name = str((t or {}).get("hpc") or "jzzn")`，两棵树都改。
   **前后对照（v2.0 实测，同一份 `hpc: 3090` 配置）**：修复前 `TypeError` 崩，修复后
   `init` rc=0（并确认该配置里 hpc 的 Python 类型就是 int）；v1.0 全技能冒烟修前 11/18 →
   修后 16/18。
2. **技能里写死裸 `tf`**（见下节 §5.4g）：副本树 `python3 bin/tf` 调用时必然
   `[Errno 2]`；PATH 里有 tf 时还可能打到**另一棵树**。

> 顺带一条踩坑记录（不是产品缺陷）：隔离配置里忘了 `remote_path_prefix:
> /home/wangchaoyue852/software/pybin`，3090 上的 gen 就找不到 numpy（`te-screen` 报
> `ModuleNotFoundError: No module named 'numpy'`）。生产配置 `setting/tf.yaml` 里有这一行，
> 自建隔离配置时要一起带上。

## 5.4h 全技能 Si 真跑（L2，真提交）

L1 只验到"输入生成"。L2 把每个技能**第一个可提交步骤**真跑一遍：

```bash
python3 tmp/test_v1_skills_si_submit.py                    # 全部（除 te-screen/需上游的）
python3 tmp/test_v1_skills_si_submit.py --tt kl-dft-cpu     # 挑技能
python3 tmp/test_v1_skills_si_submit.py --dry-run --tt X    # 只准备+retry，不提交
```

- jzzn 的 VASP 类（band/defect/elastic/ke/kl/mlff-mace/opt/phonon-dft-cpu）一律走
  **项目级 4 核覆盖**：`project_setting/templates/submit_std_3d.tpl`（`--ntasks-per-node=4`）
  + 各步 `incar_3d.tpl` 的 `NCORE=4 / KPAR=1` → `retry` 重新生成 → `start`。**站点模板不动**，
  与 §5.5 那套 4 核验证同源；远程实测 `submit.sh` 与 `INCAR` 都已是 4 核配置。
- 其余技能（MACE 类在 jzzn 走 mace_cpu venv、在 3090 走 GPU；unihamgnn 在 3090）按各技能
  出厂模板提交，不做核数改动。
- 只 `retry`/`start`，**绝不 rerun/clean**；跑完 `fetch` + `tf prove -j STEP --verify` 核验档案。
- 结果表 `tmp/v1_si_all_skills_submit.md`，逐技能输出 `tmp/si_skills_submit_logs/<技能>.log`。

### 结果（2026-09-15）

| 组 | 技能 | 结果 |
|---|---|---|
| 3090 GPU/图数据 | kl-mace-gpu、opt-mace-gpu、phonon-mace-gpu、unihamgnn | **4/4 OK**（GPU MACE 弛豫、HamGNN 图数据），tf 自动落 provenance |
| jzzn 4 核 VASP | opt-dft-cpu、elastic-dft-cpu、phonon-dft-cpu、band-dft-cpu、ke-dft-cpu | **5/5 converged**，远端实测 `--ntasks-per-node=4` + `NCORE=4 KPAR=1` |
| jzzn 按技能设计核数 | mlff-mace（step.conf 有意压到 12 核） | OK converged |
| jzzn 排队中 | kl-mace-cpu、opt-mace-cpu、phonon-mace-cpu、defect-dft-cpu | 生产队列 `PD(Priority)`，**不是失败**（defect 已按 4 核重交，job 3839848） |

即：**11 项真跑通过**（含 5 项经 4 核正式机制提交的 VASP 计算），4 项在排队；加上 L1 的
生成级覆盖，18 个技能里除 te-screen（按要求不做）与 cohp-cogito / fc-fit（需上游产物）
之外都真刀真枪跑过了。报告 `tmp/v1_si_all_skills_submit.md`。

**harness 自己踩的两个坑（已修，写进脚本注释）**：

1. **提交模板名各技能不同**：多数技能是 `submit_std_3d.tpl`，defect-dft-cpu 的 S1_bulk 走
   `submit_ncl_3d.tpl`（vasp_ncl），且它的 INCAR 来自 `incar_defect.tpl` 而不是 `incar_3d.tpl`。
   只认一个名字会出现"改了 A 模板、跑的却是 B"——首次跑 defect 就生成过
   "4 个 MPI 进程 + INCAR `NCORE=6`"这种自相矛盾的输入。现在按技能 `gen_need` 逐个覆盖，
   且任何深度的 `submit*.tpl` / `incar*.tpl` 都改。
2. **mlff-mace 有意压到 12 核**（`step1_relax/step.conf` 注释"12 核小作业（好排队）"），
   不该被 4 核覆盖——已从 4 核名单里拿掉。
3. 另有一次是 harness 自身的替换串写坏（`\g<1>4` 被吃成 `g<1>4`，生成出没有 ntasks 的
   submit.sh）。**改完必须回读远端 `submit.sh`/`INCAR` 核对**——这条已固化成报告里的两列。

### 本轮由它抓出的第三个缺陷：unihamgnn 少了 `submit_required`，S1_graph 永远提交不出去

`start` 直接被拦：`远端提交前检查失败：本地仍缺少 INCAR, KPOINTS`。根因是
`_remote_submit_preflight` 的默认清单按 VASP 要 `INCAR/POSCAR/KPOINTS/submit.sh`、按 MACE
要 `POSCAR/submit.sh`，**unihamgnn 既不跑 VASP 也不带 "mace"**，于是拿 VASP 清单去查一个
纯 Python/MPI 步骤（gen 只写 `submit.sh` + OpenMX 输入 yaml），必然缺 INCAR/KPOINTS。
核心早就留了扩展点——技能可在 `skill.yaml` 里自报 `submit_required`（`cohp-cogito`、
`fc-fit` 都写了），**unihamgnn 漏了**。
修法：`submit_required: [POSCAR, submit.sh, graph_data_gen.yaml, poscar2openmx.yaml]`
（按 gen 实际写出的文件写），两棵树都改；复验：`start` 成功拿到 3090 作业号。

## 5.4i 统一化：所有技能走同一套核数与提交前检查（2026-09-15）

L2 真跑暴露的不是"某个技能写错了"，而是**同一件事每个技能一套做法**：

| 事项 | 统一前 | 统一后 |
|---|---|---|
| 改核数 | 改 `setting/<hpc>/templates/submit_*.tpl` 的 `--ntasks-per-node` + `defects_common.build_job` 的 NCORE/KPAR，**还得知道每个技能用哪个模板名**（defect=`submit_ncl_3d.tpl`、mlff-mace=step1_relax/ 下那份） | 配置里写一个 `cores: N`（`tf.yaml` 全局 / `task_types.<tt>.cores` / 项目 `setting.yaml` / 项目或技能 `hpc.yaml`），gen 结束后 tf **按文件模式**统一归一化 |
| 提交前必须有哪些输入 | 按技能硬编码：VASP 四件套 / 名字带 mace 的两件 / 技能自报 `submit_required`（漏写就永远提交不出去——unihamgnn 就是这么卡住的） | tf 只读 `find` 一次**远端步骤目录里实际存在的文件**（作业还没跑，目录里就是 gen 写的输入），自动回拉 + 校验；`submit_required` 退化为"要精确限定时才写"的覆盖项 |

新增代码（两棵树逐字一致）：`tfpkg/workflow.py` 的 `_CORES_NORMALIZER`、
`resolve_cores`、`_append_cmd`、`_cores_cmd`、`_STEP_OUTPUT_NAMES`、
`_step_input_name_ok`、`_discover_step_inputs`；`remote_gen` 尾部接归一化，
`_remote_submit_preflight` 的默认清单换成统一发现。

归一化规则（`cores: N`）：

- `submit*.sh/slurm/tpl`：有 `--ntasks[-per-node]` → 设成 N 且 `--cpus-per-task=1`
  （避免 N×K 核超订）；只有 `--cpus-per-task` 的单进程多线程型（MACE）就把它设成 N；
  `mpirun -np $SLURM_NTASKS` 不动。
- `INCAR` / `INCAR.*`（分阶段 INCAR 一起）：`NCORE` = N 的最大 ≤4 因子（必须整除进程数，
  N=6→3、N=12→4、N=1→1），`KPAR = 1`。
- 遍历深度 1（扇出帧 `step4_disp/deform-*/submit.sh` 一起改）；不配 `cores` = 完全不动，
  出厂行为零变化。
- **不动远端文件以外的任何东西**：不 sed 本地模板、不新增仓库文件（小程序 base64 推过去跑完即删）。

### 验证

- `tmp/test_v1_uniform.py`（`v2.0: tmp/test_v2_uniform.py`）**33/33 PASS**：三种 submit 风格、
  扇出子目录、分阶段 INCAR、NCORE 因子规则、`cores` 四级优先级、"0 视为未配置"、
  清单过滤规则、真实远端发现（unihamgnn 23 个输入）、真实 `_remote_submit_preflight` 通过。
- 真集群反证：defect-dft-cpu **删掉项目级模板覆盖、回到出厂 `submit_ncl_3d.tpl`(24 核) +
  `incar_defect.tpl`(NCORE=6/KPAR=4)`** 后，只靠配置 `cores: 4` 执行 `retry`，远端实测
  `--ntasks-per-node=4` + `NCORE=4 KPAR=1`，gen 日志出现 `[cores] 本步按 cores=4 提交`。
- 统一清单反证：提交 defect 时本地 result 被自动补齐 `INCAR KPOINTS POSCAR POTCAR
  submit.sh queue.out`——正是统一发现列出来的清单。
- **新材料端到端**（`Si_uniform`，只放一个 POSCAR + 拷贝来的 project_setting）：`start`
  一条命令走完 gen（统一 cores）→ 提交前检查（统一清单）→ 提交 → 跑完，
  `S1_opt OK converged（3/2 段）`。提交前自动回拉到本地的输入是
  `INCAR INCAR.s1_a INCAR.s2_b KPOINTS POSCAR POTCAR run_relax.sh submit.sh workflow_method.txt`
  ——**分阶段 INCAR 与 run_relax.sh 这种"老四件套"看不见的输入也进了清单**，
  这正是统一发现相对硬编码清单的价值。

### 技能侧随之简化

`skill/unihamgnn/skill.yaml` 里刚加的 `submit_required` **删掉了**（统一发现已覆盖，
技能不必自报）；`cohp-cogito` / `fc-fit` 保留声明，用作"必须带上游产物"的精确表达。
L2 测试脚本 `tmp/test_v1_skills_si_submit.py` 也不再有"改模板"逻辑（`VASP_4CORE` 只用于报告标注）。

### L2 最终结果（2026-09-15 05:04 复核）

**13/14 真跑通过**：3090 的 kl/opt/phonon-mace-gpu + unihamgnn，jzzn 的 kl/opt/phonon-mace-cpu
（MACE 单进程作业排队后已跑完）+ opt/elastic/phonon/band/ke-dft-cpu（4 核）+ mlff-mace（12 核）。
唯一非 OK 的 defect-dft-cpu 是**我自己造成的**：中途 `retry` 取消了排队作业，步骤停在
`FAIL / OUTCAR missing`，重交需要 `start -f`（破坏性标志，等用户同意）。报告
`tmp/v1_si_all_skills_submit.md`，盯队列脚本 `tmp/watch_l2_queue.sh`（日志 `tmp/_si_l2_watch.log`）。

## 5.4j defect-dft-cpu 一路修到能跑——又抓出两个出厂缺陷（2026-09-15）

L2 里唯一没跑通的就是 defect-dft-cpu。重交过程中连爆三个问题，**都不是统一化改动引入的**，
是出厂就有的；只有"真提交 + 真看作业日志"才能发现：

1. **ncl 提交模板的环境与它实际执行的二进制不匹配**。`render_vasp_template` 会把模板里的
   `vasp_ncl` 换成 `setting/jzzn.yaml` 里 `vasp.standard.ncl` 的**真实路径**（个人用
   AOCC/AOCL 编译的那份 build），但模板加载的却是 Intel oneAPI + `module load
   vasp/6.4.3-oneapi2022.3` —— 那个模块环境里没有 libscalapack/libflame/libblis：
   `vasp_ncl: error while loading shared libraries: libscalapack.so`，**6 秒 FAILED**（job 3840739）。
   修：ncl 模板改用与 `submit_std_3d.tpl` 完全相同的 AOCC/AOCL 环境。
2. **个别计算节点（cu42/cu50）的 mlx5/UCX 栈会炸**：`MPI_Init` →
   `osc_ucx_component.c: component_init()` → `ucp_init_version` →
   `uct_ib_mlx5_get_compact_av` → `mlx5dv_devx_obj_destroy` **SIGSEGV**；
   不段错误的时候更隐蔽——MPI 初始化阶段狂刷 `UCX ERROR ibv_reg_mr ... Cannot allocate
   memory`，把 `queue.out` 刷到 **114 MB / 3 分钟，而 VASP 一行业都没算**（job 3840821，
   为防刷爆家目录配额已 `tf stop`）。
   修：单节点作业（`#SBATCH --nodes=1`）加 MPI 防护
   `OMPI_MCA_osc=^ucx` + `UCX_TLS=self,sm` + `OMPI_MCA_pml=ob1` + `OMPI_MCA_btl=self,vader`；
   同一节点（cu42）重交后 OUTCAR 正常产出、`queue.out` 只有 2 KB。
3. **defect 技能出厂 `ENCUT = auto`**：`skill/defect-dft-cpu/templates/step.conf` 里写
   `ENCUT = auto`，而**同一个文件的注释**和技能内 `gen_references.py` /
   `run_isif2_static.py` 用的都是 370。VASP 不接受非数值：
   `Error reading item ENCUT from file INCAR. Error code was IERR= 5` →
   `I REFUSE TO CONTINUE WITH THIS SICK JOB`。改成 `ENCUT = 370`。
   ⇒ 这类"静态看代码看不出来、真跑才炸"的参数问题，现在有静态体检兜着：
   `tmp/test_v1_templates.py`（5/5 PASS：step.conf 里 ENCUT/SIGMA/NELM/NSW… 必须是数值、
   ncl 模板环境自洽、单节点 MPI 防护在位）。

修完之后 defect-dft-cpu S1_bulk 在 cu42（原本必挂的节点）上正常计算（OUTCAR 稳定增长、
无报错、无日志刷屏）——L2 的 14 条路径至此**全部走通**（defect 是 SOC 的 3×3×1 超胞，
NSW=60，计算本身还在跑）。

**待用户决定**：生产树 `~/software/taskflow` 里这三处（ncl 模板环境、单节点 MPI 防护、
`ENCUT = auto`）**仍然是坏的**——本轮修复只落在 v1.0 与 v2.0。要不要同样修生产树？

## 5.4k 热电聚焦：te-screen 端到端跑通 + 两处事实更正（2026-09-15）

### te-screen（热电快速筛选）两步全通

`tf -tt te-screen -p Si_te-screen start`（3090 登录节点，两步都是 `run: gen`）：
S1_feat `OK`（14 维无 DFT 特征）、S2_pred `OK`（Ridge 预测）。Si 金刚石实测输出
`result/step2_predict/te_screen_summary.json`：

    PREDICTED: true
    n_ZT_e 0.431  p_ZT_e 1.196   n_log10_PF 2.514  p_log10_PF 2.482
    merit_p 1.582  merit_n 1.056  → merit 1.582（p 型占优）

即"生成 → 秒级替代模型评分"这一层是通的；精确排名仍需回 band-dft-cpu + ke-dft-cpu 取真值。
（分层材料要按 `tf init` 生成的项目配置挂类型：手写空的 `tf_*.yaml` 不够，tf 会提示
"还没有项目挂这个类型"并给出正确做法。）

### 更正一：`ENCUT = auto` 是**副本**里的问题，不是生产树的

复核生产树 `~/software/taskflow`：`skill/defect-dft-cpu/templates/step.conf` 本来就是
`ENCUT = 370`；写 `auto` 的是 v1.0/v2.0 这两份副本（已改回 370）。上一节 §5.4j 的描述
以本条为准——生产树从未被这个问题影响。

### 更正二：生产树的 ncl 模板问题**确实存在**，已修

`~/software/taskflow/setting/jzzn/templates/submit_ncl_3d.tpl` 与两棵副本一样，是
"Intel oneAPI 模块环境 + 个人 AOCC 编译的 vasp_ncl"的不匹配组合，也没有单节点 MPI 防护。
已按 §5.4j 的两条改法修好（备份 `submit_ncl_3d.tpl.bak-20260915`）；三棵树现在一致
（`grep -c OMPI_MCA_osc` 均为 1）。

### 测试材料清理（按用户指示）

- 3090 `~/taskflow/work/tf_smoke`：整树删除；
- jzzn `/public/.../work/tf_smoke`：整树删除（defect 作业已 `tf stop`、按用户要求不再跑 defect）；
- 本地 `/home/wangchao/tf_smoke_all`：已删除；只重建了 te-screen 的验证材料
  `/home/wangchao/tf_smoke_all/Si_te-screen`（热电验收用）；
- 远端相关断言在测试脚本里改成 SKIP（不再因为材料被清掉而 FAIL）。

## 5.4l 超胞矩阵端到端验证 + 全量复测（2026-09-15）

### 一般矩阵超胞真跑（六方 Mg，kl-mace-gpu）

材料 `Mg_hex`（hcp Mg，2 原子），S2 的 step.conf 设
`SUPERCELL = 2 1 0 -1 2 0 0 0 1`（P 的行主序 9 个数，det=5）：

- `S1_relax`（3090 GPU，MACE 弛豫）→ **OK**；
- `S2_force init`（gen，hiphive MC-rattle 位移）→ **OK**，远端产物：
  `klmace_params.txt` 里 `SUPERCELL=2 1 0 -1 2 0 0 0 1`（9 个数原样传给下游
  phono3py/ShengBTE 继承），`SPOSCAR` **10 个原子 = det(P)=5 × 2** ✔，
  晶格 a' = 2a−b = (7.97, −2.76, 0)（与手算一致，微差来自 S1 弛豫后的胞），
  `phono3py_disp.yaml` + 15 帧 `POSCAR-000NN` 齐备。

即"六方体系用非对角矩阵扩胞 → 建胞 → MC-rattle 位移 → 交下游"整条链是通的
（此前只认对角 `"n n n"`，六方只能硬叠大胞）。

### 全量复测（超胞改动之后）

| 项目 | 结果 |
|---|---|
| 本地回归 12 套 | **全 PASS**（agentgate / session / dropin / autodeps / history / skillspec / correct_cli / prov / uniform 28 / supercell 26 / templates 5） |
| `tf schema --strict` | rc=0 |
| 全技能 L1 生成级（18 技能） | **16/18 通过**（cohp-cogito / fc-fit 需上游产物，与改动前一致） |

结论：超胞那批改动没有破坏任何技能的 gen；`te-screen` 两步与 kl-mace-gpu 的
S1/S2 在 3090 上都正常。

## 5.5 真机验证（jzzn，4 核，Si 小体系）

**已跑通并在真集群上验证**（详录见 `tmp/v1_si4c_verification.md`）：

- 独立配置 `tmp/tf_jzzn_si.yaml`（project_roots 只指向新建的 `v1smoke_si`、auto 全关、
  `max_jobs: 2`），与生产材料、v2.0 完全隔离；
- **4 核走正式机制**：项目级覆盖 `project_setting/templates/{submit_std_3d.tpl,
  step1_std_opt/incar_3d.tpl, step2_static/incar_3d.tpl}`（24 核 → 4 核，NCORE/KPAR 同步改
  4/1）→ `tf -j S1_opt init` 重新生成 → `tf -j S1_opt start` 提交；站点模板未动；
- **S1_opt 作业 3838838 真机 OK**（PD→R@cu21→converged 5/3 段，约 4 分钟）；
- **provenance 全链路真机验证**：tf 自动落 `provenance/step1_std_opt.json` + `history.jsonl`；
  `gen_step2_static.py` 调 `provenance_common.record` 合并工具版本/现状写成
  `step2_static/provenance.json`；`tf prove --verify` 报 "12 个输入逐字节一致 ✓"，
  5 个远端现场合成文件明确标注跳过；
- 顺带抓到并修掉 4 个 provenance 缺陷（拉不回、渲染模板误判、gen 改过的副本误判、
  脚本侧合并丢来源）+ 1 个历史遗留缺陷（`method_select.py` 没进 gen_need，见 §5.6）。

## 5.6 `method_select.py` 没进 gen_need（既有缺陷，已修，两棵树都修）

`skill/_common/opt/relax_common.py` 在 `FUNC=auto`（step.conf 出厂默认）时
`import method_select`，而 `method_select.py`（2026-09-08 新增于 `_common/opt/`）
**没有被任何 skill.yaml 的 gen_need 声明**，所以永远不会被推到超算：
任何**新材料**的 step1 在 `FUNC=auto` 下都会 `ModuleNotFoundError`。

**影响面 5 个技能**（`tmp/audit_common_deps.py` 逐条解析 gen_need + 同目录 import 闭包得出，
不是估的）：elastic / ke / kl / opt / phonon-dft-cpu；`band-dft-cpu` 不用共享的
`relax_common.py`、`mlff-mace` 自带一份并置 `MOL_BRANCH=False`，因此都不受影响
（早前"7 个技能"的说法过宽，已更正）。`mlff-mace` 那条 `mol_common.py` 命中是
`MOL_BRANCH=False` 分支下的误报，脚本里已注明。

**修法（两层，都已落地）**：

1. **点修**：5 个技能 step1 的 `gen_need` 补 `method_select.py`；
2. **系统级防护 `common_autodeps`**：gen 推送前，tf 对清单里**来自公共池**的 `.py`
   做一次同目录 `import` 闭包，缺的自动补推——以后公共池加文件忘改 gen_need 不会再炸。
   只补不减；技能目录/项目覆盖里有同名文件时不补（保持 `find_asset` 的技能优先）；
   整段包 try/except，失败只警告不阻断 gen；开关 `tf.yaml: common_autodeps: false`
   或环境变量 `TF_COMMON_AUTODEPS=0`。

**两棵树都修了**：`taskflow-v1.0`（提交 `f4fc271`+`d74876c`）与 `taskflow-v2.0`
（同样 6 个文件改动，留在工作区由你并入自己的提交）。v2.0 确实受影响：两棵树的
`relax_common.py` sha256 一致（`0590baece4b602fd…`），`method_select.py` 在 v2.0
当时还是未跟踪文件。

**验证**：

- `tmp/audit_common_deps.py`：两棵树都报 "没有未声明的同目录依赖 / 共 0 条"；
- `tmp/test_v1_autodeps.py`（14 项）：含"故意漏写 → 真跑 remote_gen 仍成功"与
  开关关闭即不补推，两棵树都 ALL PASS；
- **真机最强验证**：把 `kl-dft-cpu` 里补的那行声明**临时撤回**（回到出事时的清单），
  jzzn 上 `init` 仍成功、`method_select.py` 照样送达、`FUNC` 自动选到 `pbesol`
  （INCAR `GGA=PS`）；随后恢复原样。即"就算没做点修，防护网单独也挡得住"。

